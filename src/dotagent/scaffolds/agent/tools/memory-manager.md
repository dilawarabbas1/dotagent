# Memory Manager

CRUD + search + summarize across all four memory stores. Replaces the legacy `memory-guard.sh` and `memory-update.md` / `memory-conflict.md` prompts. Handles per-actor personal memory, content-hashed semantic writes, and append-only episodic.
