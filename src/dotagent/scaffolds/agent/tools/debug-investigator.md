# Debug Investigator

Given a stack trace or failing test, walks episodic memory across all teammates for similar past failures and proposes the historical fix. Cites the prior event(s) by id so the user can verify.
